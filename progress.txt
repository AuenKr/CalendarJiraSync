
## 2026-01-26 - Revert Date Picker & Fix Theme
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- **Reverted Date Picker**: Removed `react-day-picker` and restored the native `<input type="date">` in `src/popup/App.tsx` to fix UI issues.
- **Fixed Theme Contrast**: Updated `src/content/ContentApp.tsx` to explicitly set background and text colors for the search results popover.
  - Added `bg-white dark:bg-gray-800` to the popover content.
  - Added `dark:text-white` and `dark:text-gray-300` to the result items to ensure readability in dark mode.
- **Cleanup**: Removed unused `src/components/ui/calendar.tsx` and uninstalled `react-day-picker` and `date-fns`.
- **Build**: Verified build passes with `bun run build`.
---
