
## 2026-01-26 - Bug Fix (Search While Typing)
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Fixed issue where searching by Jira Key (e.g., "PROJ-123") was failing because the key was being stripped from the query.
  - Updated regex in `src/content/ContentApp.tsx` to only strip keys if they are wrapped in brackets (e.g., `[PROJ-123]`), allowing raw keys to be searched.
- Fixed issue where API search would fail for non-key queries (e.g., "test") due to invalid JQL generation.
  - Updated `src/background/index.ts` to only include `key = "QUERY"` in JQL if the query matches a Jira key pattern.
- Verified build passes.
---
