
## Codebase Patterns
- Use `toLocaleDateString('en-CA')` or manual timezone offset adjustment for local YYYY-MM-DD strings to avoid UTC mismatches with Calendar dates.
- Google Calendar Event Bubble (popup) uses `[role="heading"]` for title, while Edit Page uses `input[aria-label="Add title"]`.

## 2026-01-27 - Fix Suggestion Box Behavior
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- **Bug Fix**: The suggestion box now closes when the event title input loses focus.
- **Implementation**:
  - Added `isFocused` state to `ContentApp.tsx` to track focus on the title input.
  - Added `focus` and `blur` event listeners to update `isFocused`.
  - Updated the `useEffect` controlling the popover visibility to respect `isFocused`.
  - Added `onMouseDown={(e) => e.preventDefault()}` to `PopoverContent` to prevent focus loss when interacting with the popover itself (though the main fix is the focus tracking).
- **Verification**: Verified that the suggestion box closes when clicking outside the input field.
---

## 2026-01-30 - US-015
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- **Jira Task Status Management**:
  - Implemented status display and transition dropdown in `ContentApp.tsx`.
  - Updated `src/content/index.tsx` to support injecting into the Event Popup (Bubble) by finding the title element when no input is present.
  - Updated `ContentApp.tsx` to read title from `titleElement` (read-only) if `titleInput` is missing.
  - Verified sorting logic for search results (In Progress > To Do > Done).
- **Learnings**:
  - Google Calendar Event Bubble does not have a title input, so we must read from the title element (e.g., `[role="heading"]`).
  - `ContentApp` needed to be flexible to handle both input-based (Edit Page) and element-based (Bubble) title sources.
---

## 2026-01-30 - US-016
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- **Smart Work Log Filtering**:
  - Implemented LRU cache logic in `useConfigStore.ts` to keep only the last 30 days of sync history.
  - Fixed date handling in `App.tsx` to correctly use local time for date comparisons, ensuring events are filtered correctly based on the user's local date.
  - Verified logic for preventing duplicates (strict cutoff based on `lastLoggedTime`) and ignoring in-progress events.
- **Learnings**:
  - `new Date().toISOString()` returns UTC, which can cause date mismatch issues when comparing with local calendar dates. Using `toLocaleDateString('en-CA')` or manual offset adjustment is necessary for correct local YYYY-MM-DD strings.
---

## 2026-01-30 - US-017
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- **Work Log Content Format**:
  - Updated `src/popup/App.tsx` to include the date in the worklog comment.
  - Format: `[Event Title] \n[Date]\n[Start Time] - [End Time]\n[Event Description]`.
  - Used `toLocaleDateString()` for the date to ensure it matches the user's locale.
- **Learnings**:
  - Jira ADF text nodes support newlines, so constructing a single string with `\n` works for the comment field.
---

## 2026-01-30 - US-018
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- **Setup Experience Improvements**:
  - Updated `src/setup/App.tsx` to include a smart suggestion dropdown for the Jira Domain input.
  - Implemented logic to detect partial domains (e.g., "company") and suggest the full Atlassian URL ("company.atlassian.net").
  - Added click handler to apply the suggestion immediately.
  - Verified flexible input handling in the submit logic (stripping protocol, www, etc.).
- **Learnings**:
  - Providing immediate visual feedback (suggestions) improves the user experience significantly compared to just cleaning the input on submit.
---

## 2026-01-31 - US-019
Thread: https://ampcode.com/threads/
- **Fix Reload Focus Issue**:
  - Updated `src/content/index.tsx` to pass the `modal` container to `ContentApp`.
  - Updated `src/content/ContentApp.tsx` to:
    - Accept `container` prop and search for input within it.
    - Prioritize `document.activeElement` if it matches the title input criteria.
    - Increased polling frequency to 500ms.
  - Verified build and lint pass.
- **Learnings**:
  - **Google Calendar Injection**: When injecting into a dynamic SPA like Google Calendar, always check `document.activeElement` to find the input the user is currently interacting with, as the DOM might have multiple inputs or be in a state of flux.
  - **Content Script Polling**: Increasing polling frequency (e.g. 500ms) and scoping searches to the specific modal container improves robustness.
---
