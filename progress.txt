
## 2026-01-26 - UI Improvements & Bug Fixes
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- **Spinning Sync Button**: Updated `src/popup/App.tsx` to use `isFetching` instead of `isLoading` for the project refresh button spinner. This ensures the spinner appears even when refreshing existing data.
- **Event Title Update Fix**: Updated `src/content/ContentApp.tsx` to improve how the event title is updated when linking a task.
  - Now uses the native property setter (`Object.getOwnPropertyDescriptor`) to bypass React/framework wrappers.
  - Dispatches `input` and `change` events.
  - Focuses the input before updating and blurs it afterwards (with a slight delay) to simulate user interaction and trigger Google Calendar's internal save logic.
- Verified build passes.
---
